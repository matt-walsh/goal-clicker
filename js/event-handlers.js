/*
    Project: Goal clicker
    Filename: event-handlers.js
    Author: Matt Walsh
    Date: 2019-11-25
    Description: Hook up on click events.
*/

window.onload = (event) => {
    //Attach Event Handlers to the Inputs
    let clickerButton = document.getElementById("clicker");
    let startGenerator = document.getElementById("start-generator");
    let stopGenerator = document.getElementById("stop-generator");

    clickerButton.addEventListener("click", (event) => {
        
    });

    startGenerator.addEventListener("click", (event) => {
        
    });

    stopGenerator.addEventListener("click", (event) => {
        
    });
};